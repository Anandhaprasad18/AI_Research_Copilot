app = "REPLACE_WITH_YOUR_FLY_APP_NAME"
kill_signal = "SIGINT"
kill_timeout = 5

[env]
  PORT = "8000"

[[services]]
  internal_port = 8000
  protocol = "tcp"
  processes = ["app"]
  [[services.ports]]
    handlers = ["http"]
    port = 80
  [[services.ports]]
    handlers = ["tls"]
    port = 443
